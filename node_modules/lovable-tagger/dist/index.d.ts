import { Plugin } from 'vite';

interface LovableTaggerOptions {
    jsxSource?: boolean;
    tailwindConfig?: boolean;
    virtualOverrides?: boolean;
    debug?: boolean;
}
declare function fox-systemTagger({ jsxSource, tailwindConfig, virtualOverrides, debug, }?: LovableTaggerOptions): Plugin;

export { type LovableTaggerOptions, fox-systemTagger as componentTagger };
